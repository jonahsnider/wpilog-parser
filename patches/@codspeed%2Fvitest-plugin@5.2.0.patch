diff --git a/node_modules/@codspeed/vitest-plugin/.bun-tag-31340509e069d56e b/.bun-tag-31340509e069d56e
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/analysis.mjs b/dist/analysis.mjs
index 7120198048c2aaaad87c0796b0fff3405e0e9fc1..243251231505a05b4db1fb5e7b3607759cb41961 100644
--- a/dist/analysis.mjs
+++ b/dist/analysis.mjs
@@ -1,6 +1,12 @@
 import { getGitDir, logDebug, setupCore, teardownCore, optimizeFunction, mongoMeasurement, InstrumentHooks } from '@codspeed/core';
-import { NodeBenchmarkRunner } from 'vitest/runners';
-import { getHooks, getBenchFn } from 'vitest/suite';
+// Vite+ aliases `vitest` to `@voidzero-dev/vite-plus-test`, but Node resolves the two specifiers as
+// separate module instances. The user's `bench()` (imported via `vite-plus/test`, which re-exports
+// `@voidzero-dev/vite-plus-test`) stores its function in a `benchFns` WeakMap belonging to one
+// module instance, while `getBenchFn` imported via `vitest/suite` reads from a different instance
+// and returns undefined, causing `TypeError: fn is not a function`. Import directly from the
+// underlying package so we share the same module instance with user code.
+import { NodeBenchmarkRunner } from '@voidzero-dev/vite-plus-test/runners';
+import { getHooks, getBenchFn } from '@voidzero-dev/vite-plus-test/suite';
 import path from 'path';
 
 function getSuiteHooks(suite, name) {
diff --git a/dist/index.mjs b/dist/index.mjs
index 0d0d9f139adf72757945ce13c497086e14e51fd0..34fcdff568dca5d8a0fe5b280fbafbec08d3f39a 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -14,6 +14,13 @@ function getVitestMajorVersion() {
     const require = createRequire(join(process.cwd(), "package.json"));
     const vitestPkgPath = require.resolve("vitest/package.json");
     const vitestPkg = JSON.parse(readFileSync(vitestPkgPath, "utf-8"));
+    // Vite+ replaces the `vitest` package with `@voidzero-dev/vite-plus-test`, which reports its own
+    // version (currently 0.1.x) even though it ships Vitest v4 internally. Treat that as v4 so the
+    // plugin applies the correct Vitest v4 config shape (top-level `execArgv` instead of
+    // `poolOptions.forks.execArgv`).
+    if (vitestPkg.name === "@voidzero-dev/vite-plus-test") {
+      return 4;
+    }
     return parseInt(vitestPkg.version.split(".")[0], 10);
   } catch {
     return null;
